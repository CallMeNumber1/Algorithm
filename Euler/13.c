#include <stdio.h>
#define MAX 100

int num[MAX] = {1, 0};
int main() {
    int n = 100;
    char str[MAX + 5];
    for (int i = 0; i < n; i++) {
        scanf("%s", str);
        for (int i = 1; i <= num[0]; i++) {
            num[i] += 
        }
    }

    return 0;
}